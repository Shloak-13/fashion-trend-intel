# Phase 1 — ingestion, storage, testing
# NLP (spacy, transformers, torch) and dashboard (streamlit) deps are added in Phase 2/4.
#
# Phase 2 note: CLAUDE.md Section 12 pins (keybert==0.8.1, sentence-transformers==2.7.0,
# torch==2.3.0) are unpinned/outdated for Python 3.13. Using latest-compatible versions
# instead — verified to install and import cleanly.

# Data ingestion
pytrends==4.9.2
praw==7.7.1
requests==2.31.0
beautifulsoup4==4.15.0
lxml==6.1.3
protego==0.6.2

# Data processing
pandas==2.2.2
numpy==1.26.4
sqlalchemy==2.0.36
python-dotenv==1.0.1

# Scheduling
apscheduler==3.10.4

# Testing
pytest==8.2.0
pytest-cov==5.0.0

# Utilities
loguru==0.7.2
tqdm==4.66.4

# NLP (Phase 2)
keybert==0.9.0
sentence-transformers==6.0.1
rapidfuzz==3.14.6
langdetect==1.0.9

# Notebooks (Phase 2)
ipykernel==7.3.0
nbconvert==7.17.1
jupyter_client==8.10.0
