# Phase 1 — ingestion, storage, testing
# NLP (spacy, transformers, torch) and dashboard (streamlit) deps are added in Phase 2/4.

# Data ingestion
pytrends==4.9.2
praw==7.7.1
requests==2.31.0

# Data processing
pandas==2.2.2
numpy==1.26.4
sqlalchemy==2.0.36
python-dotenv==1.0.1

# Scheduling
apscheduler==3.10.4

# Testing
pytest==8.2.0
pytest-cov==5.0.0

# Utilities
loguru==0.7.2
tqdm==4.66.4
